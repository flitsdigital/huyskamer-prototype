The brand's recurring header lockup: handwritten script eyebrow over a bold PT-Serif heading.

```jsx
<SectionHeading eyebrow="Ontdek onze" title="Onze menukaart"
  description="Stap binnen bij De Huyskamer en geniet van de huiselijke sfeer." />
```

Props: `eyebrow`, `title`, `description`, `align` (left/center/right), `onDark`.
