White text field with the brand's handwritten label, sat on a sand panel.

```jsx
<Input label="Naam" placeholder="Jordi Klavers" />
<Input label="Jouw Email" type="email" error="Vul een geldig e-mailadres in" />
```

Props: `label` (script), `hint`, `error`, plus all native input attributes.
